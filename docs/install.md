# Установка

Целевая платформа — один VPS с Linux, Docker и публичным IP.
Минимум: 2 vCPU, 4 ГБ RAM, 20 ГБ диска. Транскодирование добавляет примерно
одно ядро на камеру, поэтому при H.265-камерах считайте CPU отдельно.

## Быстрый путь: install.sh

На чистом сервере всё делается одной командой:

```bash
sudo bash install.sh
```

Первым вопросом скрипт предложит выбрать, как вы будете открывать панель:
по своему домену, по IP через sslip.io или просто по IP со своим
сертификатом — **домен не обязателен** (подробности ниже). Дальше он спросит
почту, политику 2FA, параметры SMTP (необязательно — нужен только для
приглашений сотрудников) и адрес администратора; проверит DNS; поставит Docker из
официального репозитория; сгенерирует пароль базы и ключ шифрования; настроит
файрвол (первым делом разрешив ваш текущий SSH-порт); соберёт образы, накатит
миграции и предложит задать пароль администратора.

Повторный запуск безопасен: существующий `.env` можно оставить, ключ
шифрования не перезаписывается никогда, а `docker compose up` идемпотентен.

Ниже — то же самое вручную, если нужен контроль над каждым шагом.

## 1. Подготовка сервера

```bash
sudo apt update && sudo apt install -y docker.io docker-compose-plugin git
sudo systemctl enable --now docker
```

### Адрес сервиса: домен не обязателен

Поддерживаются три режима. Установщик спрашивает про них первым делом;
вручную режим задаётся двумя переменными в `.env`.

| Режим | `DOMAIN` | `TLS_ISSUER` | Сертификат |
|---|---|---|---|
| Свой домен | `cam.company.ru` | `admin@company.ru` | Let's Encrypt, доверенный |
| Домена нет, IP публичный | `203-0-113-10.sslip.io` | `admin@company.ru` | Let's Encrypt, доверенный |
| Только IP | `203.0.113.10` | `internal` | свой CA, браузер предупреждает |

**Свой домен.** `A`-запись должна указывать на IP этого сервера — иначе
Let's Encrypt не сможет проверить владение.

**Домена нет.** [sslip.io](https://sslip.io) — публичный DNS-сервис, который
резолвит имя `203-0-113-10.sslip.io` обратно в `203.0.113.10`. Для Let's
Encrypt это обычное доменное имя, проверка проходит по HTTP-01, и вы получаете
нормальный сертификат без покупки домена. Зрители не увидят никаких
предупреждений.

Цена решения: работоспособность зависит от стороннего DNS-сервиса. Если
sslip.io станет недоступен, уже выпущенный сертификат продолжит работать до
конца срока (90 дней), но обновиться не сможет. Для сервиса, которым
пользуется компания, домен всё же надёжнее.

**Только IP.** Caddy выпускает сертификат собственным центром сертификации.
Шифрование полноценное, но браузер ругается, пока корневой сертификат не
установлен на машины зрителей. Забрать его:

```bash
docker compose cp caddy:/data/caddy/pki/authorities/local/root.crt ./rtspgw-ca.crt
```

Этот режим — единственный возможный, если сервер стоит во внутренней сети с
приватным адресом: до `10.0.0.5` Let's Encrypt не достучится. Для раздачи
ссылок наружу он плохо подходит: получатель увидит предупреждение и, скорее
всего, решит, что ссылка поддельная.

> Let's Encrypt с 2025 года выдаёт сертификаты и на голые IP-адреса, но только
> через профиль `shortlived` (6 дней), и поддержка этого в Caddy пока
> нестабильна. Поэтому такой путь здесь не используется.

Сменить режим потом можно в любой момент: поправить `DOMAIN` и `TLS_ISSUER`
в `.env` и перезапустить Caddy. Ранее выданные публичные ссылки при этом
перестанут работать — в них зашит старый адрес.

### Файрвол

Наружу открыто ровно три порта. Всё остальное — включая Control API MediaMTX,
Postgres, Redis и порты HLS/WebRTC-сигналинга — доступно только внутри
docker-сетей.

```bash
sudo ufw allow 22/tcp        # ssh
sudo ufw allow 80/tcp        # ACME-проверка и редирект на https
sudo ufw allow 443/tcp       # панель, плеер, медиа
sudo ufw allow 8189/udp      # медиа WebRTC
sudo ufw allow 8189/tcp      # тот же WebRTC там, где закрыт UDP
sudo ufw enable
```

## 2. Конфигурация

```bash
git clone <репозиторий> /opt/rtspgw && cd /opt/rtspgw
cp .env.example .env
```

Отредактируйте `.env`: адрес сервиса (`DOMAIN`), способ получения сертификата
(`TLS_ISSUER` — см. таблицу выше), `PUBLIC_HOST` для WebRTC и пароль Postgres.
Пароли удобно сгенерировать так:

```bash
openssl rand -base64 30
```

### Ключ шифрования

Учётные данные камер и TOTP-секреты лежат в базе зашифрованными. Ключ живёт
отдельным файлом и монтируется как docker secret.

```bash
openssl rand -base64 32 > secrets/app_key
chown 10001:10001 secrets/app_key && chmod 400 secrets/app_key
```

`chown` обязателен. Docker монтирует файл секрета в контейнер как есть,
сохраняя владельца и права хоста, а процесс приложения работает под
непривилегированным uid 10001 (см. `useradd` в `backend/Dockerfile`).
Файл, принадлежащий root с правами 600, он прочитать не сможет и упадёт
на старте с `Permission denied`.

**Сохраните копию ключа в менеджере паролей компании.** Без него дамп базы
бесполезен: камеры придётся заводить заново.

### hls.js (фолбэк для Chrome и Firefox)

```bash
bash ops/fetch-vendor.sh
```

Без этого шага сервис работает, но в Chrome и Firefox не будет запасного
HLS-варианта, если в сети зрителя закрыт UDP.

## 3. Первый запуск

```bash
docker compose up -d --build
docker compose run --rm api alembic upgrade head
docker compose exec -it api python -m app.cli create-admin --email admin@company.ru
```

Проверка:

```bash
docker compose ps
```

```bash
curl -s https://cam.company.ru/healthz
```

Все контейнеры должны быть `healthy`, `/healthz` — отвечать `ok`.

## 4. Первая камера

Откройте `https://<домен>/`, войдите, нажмите «Добавить камеру» и вставьте
RTSP-ссылку:

```
rtsp://логин:пароль@31.148.246.249:4259/stream
```

Спецсимволы в логине и пароле записывайте в процентной кодировке:
`@` → `%40`, `:` → `%3A`, `#` → `%23`, `/` → `%2F`.

В течение минуты в карточке камеры появится результат пробы кодеков. Если
там написано, что нужен транскод, — переключите профиль в настройках камеры.

Дальше: «Создать ссылку» на карточке камеры. Ссылка показывается **один раз** —
в базе хранится только её хеш.

## 5. Сотрудники: приглашения по почте

«Пользователи» → «Пригласить»: адрес, имя, роль. Сотруднику уходит письмо со
ссылкой, по которой он задаёт себе пароль, — временные пароли не пересылаются
ни письмом, ни в мессенджере. Ссылка одноразовая и живёт `INVITE_TTL_HOURS`
(по умолчанию 72 ч).

Пока приглашение не принято, учётной записи не существует: войти под этим
адресом нельзя. В таблице «Приглашения» видно, ушло ли письмо; там же —
«Выслать заново» (перевыпускает ссылку, старая перестаёт работать) и
«Отозвать».

### SMTP

Задаётся в `.env` (полный список полей — в `.env.example`):

```
SMTP_HOST=smtp.company.ru
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_USERNAME=noreply@company.ru
SMTP_PASSWORD=...
MAIL_FROM=noreply@company.ru
```

После правки: `docker compose up -d api worker`.

Пароль можно держать не в `.env`, а файлом — `SMTP_PASSWORD_FILE=/run/secrets/
smtp_password`, добавив секрет в `docker-compose.yml` по образцу `app_key`.

Контейнер `api` ходит к SMTP через сеть `core` (у неё есть выход наружу);
сеть `data` изолирована и к почте отношения не имеет.

**Почта необязательна.** Если `SMTP_HOST` пуст или SMTP не ответил,
приглашение всё равно выпускается, а панель показывает ссылку прямо на
странице — передайте её сотруднику сами. Отправка ждёт SMTP не дольше
`SMTP_TIMEOUT_SECONDS` (15 с), чтобы медленный сервер не подвешивал панель.

Проверить, что почта настроена, проще всего пригласив самого себя на второй
адрес. Отказы SMTP видны в журнале аудита как `invite.send_failed` и в логах:

```bash
docker compose logs api | grep smtp_
```

## 6. Второй фактор

По умолчанию (`TOTP_POLICY=admins`) администраторов после входа сразу ведёт
на настройку 2FA. Чтобы требовать её со всех, поставьте `TOTP_POLICY=all`
и перезапустите `api`. Приглашённый сотрудник настраивает 2FA при первом
входе — приглашение задаёт только пароль.

## 7. Регулярные задачи

```bash
sudo crontab -e
```

```
0 3 * * * /opt/rtspgw/ops/backup.sh >> /var/log/rtspgw-backup.log 2>&1
```

Мониторинг (необязательно):

```bash
docker compose -f docker-compose.yml -f docker-compose.monitoring.yml up -d
```

Grafana слушает только `127.0.0.1:3000` — открывайте её через SSH-туннель.

## Обновление

```bash
git pull && docker compose up -d --build && docker compose run --rm api alembic upgrade head
```

При обновлении образа MediaMTX сверьте набор ключей в `mediamtx/mediamtx.yml`
с `mediamtx.yml` соответствующего тега: на незнакомом ключе MediaMTX не
стартует. Пути при этом не потеряются — реконсилятор восстановит их из базы.
