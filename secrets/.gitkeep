# Сюда кладётся app_key — ключ шифрования учётных данных камер.
# Сам ключ в репозиторий не попадает (см. .gitignore).
#
#   docker compose run --rm --no-deps api python -m app.cli gen-key > secrets/app_key
#   chmod 600 secrets/app_key
